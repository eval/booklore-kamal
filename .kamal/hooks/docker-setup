#!/bin/sh

for host in $(echo "$KAMAL_HOSTS" | tr ',' ' '); do
  echo "Creating data directories on $host..."
  ssh root@$host 'mkdir -p /data/booklore/{data,books,bookdrop,mariadb} && chown -R 1000:1000 /data/booklore/{data,books,bookdrop}'
done
